# This Vercel project deploys ONLY the static Next.js export (out/).
# The Hono API (api/) is a separate service and must NOT be uploaded — with a
# non-Next framework preset Vercel would treat each api/**/*.ts file as a
# Serverless Function and blow the Hobby 12-function limit.
api/
supabase/
docs/
