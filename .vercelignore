# The Hono API source (api/src) is bundled into the Next.js build via the
# /api catch-all route, so it MUST be uploaded. Only exclude what the build
# does not need.
supabase/
docs/
api/tests/
api/dist/
api/node_modules/
